#!/bin/sh

if pgrep -f thin; then
  command=restart
else
  command=start
fi

export RACK_ENV=production
echo "restarting Thin"
./bin/thin --environment production --socket /var/tmp/bitcoinppi.org.sock --pid tmp/thin.pid --log logs/thin.log --daemonize $command

